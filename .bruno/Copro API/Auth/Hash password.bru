meta {
  name: Hash password
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{apiVersion}}/auth/hash/password
  body: json
  auth: inherit
}

body:json {
  {
    "password": "test"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
